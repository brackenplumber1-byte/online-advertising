# Copy to <slug>.env and fill in only the platforms set up so far for this
# business. Leave a platform's vars unset to fall back to draft-only mode.

# --- Meta (Facebook + Instagram) — see ../references/meta.md ---
META_PAGE_ID=
META_PAGE_ACCESS_TOKEN=
META_IG_USER_ID=

# --- LinkedIn — see ../references/linkedin.md ---
LINKEDIN_ACCESS_TOKEN=
LINKEDIN_AUTHOR_URN=

# --- Google Business Profile — see ../references/gbp.md ---
GBP_ACCOUNT_ID=
GBP_LOCATION_ID=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
